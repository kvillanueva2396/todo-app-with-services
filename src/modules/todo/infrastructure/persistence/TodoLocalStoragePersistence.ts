import Todo from '../../domain/entities/Todo';
import { TodoRepository } from '../../domain/repository/TodoRepository';

export default class TodoLocalStoragePersistence implements TodoRepository {}
